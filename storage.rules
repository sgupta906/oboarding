rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow all reads/writes in development (emulator)
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
